<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             x:Class="suota_pgp.KeyBlobView"
             prism:ViewModelLocator.AutowireViewModel="True"
             Title="Key Extractor">
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Get Key/Blob" 
                    Command="{Binding GetKeyBlobCommand}"/>
            <StackLayout Orientation="Horizontal">
                <Label Text="Key: "/>
                <Label Text="{Binding KeyBlob.Key}"/>
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Blob: "/>
                <Label Text="{Binding KeyBlob.Blob}"/>
            </StackLayout>
            <Button Text="Save"
                    Command="{Binding SaveCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>