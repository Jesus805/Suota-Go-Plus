<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             x:Class="suota_pgp.DeviceInfoView"
             xmlns:local="clr-namespace:suota_pgp"
             prism:ViewModelLocator.AutowireViewModel="True"
             Title="Key Extractor">
    <ContentPage.Resources>
        <ResourceDictionary>
            <local:ScanStateToBoolConverter x:Key="ss2b"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition />
                <RowDefinition Height="auto"/>
                <RowDefinition />
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Text="Scan"
                    Command="{Binding ScanCommand}"
                    IsVisible="{Binding State,
                                        Converter={StaticResource ss2b},
                                        ConverterParameter='inverse'}"
                    HorizontalOptions="Center"/>
            <Button Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Text="Stop Scanning"
                    Command="{Binding StopScanCommand}"
                    IsVisible="{Binding State, 
                                        Converter={StaticResource ss2b}}"
                    HorizontalOptions="Center"/>
            <ListView Grid.Row="1"
                      Grid.Column="0"
                      Grid.ColumnSpan="2"
                      ItemsSource="{Binding Devices}"
                      SelectedItem="{Binding SelectedDevice}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal">
                                <Label Text="{Binding Name}"/>
                                <Label Text=" - "/>
                                <Label Text="{Binding BtAddress}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Grid.Row="2"
                    Grid.Column="0"
                    Text="Get Device Info"
                    Command="{Binding GetDeviceInfoCommand}"/>
            <Button Grid.Row="2"
                    Grid.Column="1"
                    Text="Restore PGP"
                    Command="{Binding RestoreCommand}"/>
            <Grid Grid.Row="3"
                  Grid.Column="0"
                  Grid.ColumnSpan="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" 
                       Grid.Column="0"
                       Text="Bluetooth Address: "/>
                <Label Grid.Row="0"
                       Grid.Column="1"
                       Text="{Binding DeviceInfo.BtAddress}"/>
                <Label Grid.Row="1"
                       Grid.Column="0"
                       Text="Key: "/>
                <Label Grid.Row="1"
                       Grid.Column="1"
                       Text="{Binding DeviceInfo.Key}"/>
                <Label Grid.Row="2"
                       Grid.Column="0"
                       Text="Blob: "/>
                <Label Grid.Row="2"
                       Grid.Column="1"
                       Text="{Binding DeviceInfo.Blob}"/>
            </Grid>
            <Button Grid.Row="4"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Text="Save"
                    Command="{Binding SaveCommand}"
                    HorizontalOptions="Center"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>